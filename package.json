{
  "name": "n8n-v0",
  "private": true,
  "scripts": {
    "dev:backend": "cd apps/backend && bun run dev",
    "build:backend": "cd apps/backend && bun run build",
    "prisma": "cd packages/db && bunx prisma migrate dev && bunx prisma generate",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.3",
    "@types/node": "^24.3.1",
    "prettier": "^3.6.2",
    "prisma": "^6.15.0",
    "turbo": "^2.5.6",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.2.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@prisma/client": "^6.15.0",
    "@types/cookie-parser": "^1.4.9",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.2.2",
    "react-router-dom": "^7.9.1"
  }
}
